apiVersion: v1
kind: Service
metadata:
  name: netology-svc-dep
  namespace: netology  
spec:
  ports:
    - name: web
      port: 9001
      protocol: TCP
      targetPort: 80
    - name: mtool
      port: 9002
      protocol: TCP
      targetPort: 8080
  selector:
    app: nginx-multitool
      